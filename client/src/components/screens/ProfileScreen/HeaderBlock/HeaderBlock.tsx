import clsx from 'clsx';

import styles from './HeaderBlock.module.scss';

const HeaderBlock = () => <section className={clsx(styles.root, 'block-fluid')}>Header</section>;

export default HeaderBlock;
